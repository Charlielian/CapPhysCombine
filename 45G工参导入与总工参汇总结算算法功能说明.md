# 4/5G工参导入与多源汇总结算算法功能说明文档

## 1. 业务背景与概述

在移动通信无线网络优化体系中，4G与5G工参数据通常来源于不同制式或不同厂家的网管系统。原始导出文件表头存在命名不统一、关键属性定义差异（如局站名与物理站名、频段字段与详细频段字段等）以及多天线/多分区分裂扇区等情况。

本功能实现了多制式、多格式（Excel/CSV）工参文件的统一批量导入，具备自动制式识别、异构字段映射兼容清洗、基于GIS的空间网格多级缓冲区拓扑计算、复合唯一键合并去重（Upsert），并汇总入库为标准化的全网统一总工参底表（`engineering_params`），同时支持无线射频特征参数（波瓣角与覆盖半径）的规则派生计算与多维数据导出。

---

## 2. 核心算法处理全流程

```
 ┌───────────────────────────────┐   ┌───────────────────────────────┐
 │   4G 工参源文件 (.xlsx / .csv)  │   │   5G 工参源文件 (.xlsx / .csv)  │
 └───────────────┬───────────────┘   └───────────────┬───────────────┘
                 │                                   │
                 └─────────────────┬─────────────────┘
                                   │
                                   ▼
                   【阶段一：文件解析与制式识别】
             - 格式适配：自动调用 pd.read_csv / pd.read_excel
             - 语义识别：通过文件名中的 '5g' / 'nr' 自动标识制式
                                   │
                                   ▼
                   【阶段二：异构字段动态映射】
             - 物理站名映射：5G 优先取“所属局站”，4G 优先取“所属站点名称”
             - 频段映射：4G 自适应取“使用频段”与“详细使用频段”
             - 关键射频参数优先级回退链：方位角、下倾角、挂高等
                                   │
                                   ▼
                   【阶段三：数据清洗与类型规整】
             - 空值规整：处理各种 NaN / np.nan 为安全存储值（None/空串）
             - 经纬度地理范围有效性校验 ([70, 140], [15, 55])
                                   │
                                   ▼
                   【阶段四：GIS空间网格拓扑计算】
             - 基础匹配：WGS84 下点面包含判定（包含经纬度判定）
             - 500米缓冲：投影到高斯/UTM平面坐标系进行 500m 缓冲计算
             - 拓扑去重：缓冲网格命中结果中剔除已在“不缓冲网格”中命中的网格
             - 标签关联：关联微网格分类标签（口碑格、保障格等）
                                   │
                                   ▼
                   【阶段五：多维防重合并与汇总入库 (Upsert)】
             - 复合唯一标识检查：(cgi, lon, lat, ant_dir)
             - 命中则原地更新全量属性，未命中则新增插入
                                   │
                                   ▼
                   【阶段六：统一总工参库 (SQLite / MySQL)】
                     数据表：engineering_params
                                   │
                                   ▼
                   【阶段七：衍生覆盖参数计算与应用导出】
             - 基于制式与频段的规则引擎计算 beam（波束宽度）与 radius（覆盖半径）
             - 导出带格式标准工参 Excel，提供多模块（干扰、业务突降、地图）服务
```

---

## 3. 详细算法实现机制

### 3.1 文件制式自动识别
无需用户手工指定制式，通过文件名字符串特征自动识别：
* **5G判定**：`filename.lower()` 包含 `'5g'` 或 `'nr'`，则判定该文件为 5G 工参（`zhishi = '5g'`）。
* **4G判定**：其余文件默认判定为 4G 工参（`zhishi = '4g'`）。

### 3.2 异构字段动态映射优先级链（Fallback Chain）
针对 4G/5G 系统来源不同导致的列名异构，采用如下优先级链进行智能提取：

| 标准总工参字段 | 字段含义 | 5G 映射优先级顺序 | 4G 映射优先级顺序 |
| :--- | :--- | :--- | :--- |
| `phy_name` | 物理站名称 | 所属局站 > phy_name | 所属站点名称 > phy_name |
| `pinduan` | 频段 | 使用频段 > pinduan | 使用频段 > 详细使用频段 > pinduan |
| `ant_dir` | 方位角 | 方位角 > ant_dir > 天线方位角 > 天线方向角 | 方位角 > ant_dir > 天线方位角 > 天线方向角 |
| `elect_tilt` | 电下倾角 | 电下倾角 > elect_tilt > 电子下倾角 > 电子倾角 > 电倾角 | 同左 |
| `mech_tilt` | 机械下倾角 | mech_tilt > 机械下倾角 > 机械倾角 | 同左 |
| `ant_height` | 挂高 | 挂高 > ant_height > 天线高度 > 天线挂高 | 同左 |
| `pl_item` | 规划ID | 所属规划ID > pl_item | 同左 |
| `phy_id` | 物理站ID | phy_id > 站点ID > 物理站点ID | 同左 |
| `lat` / `lon` | 纬度 / 经度 | lat/lon > 纬度/经度 > Latitude/Longitude | 同左 |

### 3.3 数据清洗与类型安全转换
1. **NaN / 空值规整**：
   * 字符串字段：`None` 或 `np.nan` 统一转为空字符串 `""`，并执行 `.strip()` 去除首尾空格。
   * 数值字段（下倾角、方位角、挂高、坐标）：尝试安全转换为 `float`；遇到空串、非数字或 `NaN` 统一置为 `None`（存为数据库 `NULL`），防止数据库操作报错。
2. **坐标有效性筛选**：
   * 检查经纬度数值范围，限定在中国大陆合理经纬度区间：$lon \in [70, 140]$ 且 $lat \in [15, 55]$，异常值跳过空间匹配。

### 3.4 GIS空间网格拓扑匹配算法（双层缓冲区模型）
利用 GeoPandas 与 Shapely 进行几何空间包含计算，分为三层逻辑：
1. **不缓冲网格匹配（Point-in-Polygon）**：
   * 在 WGS84（EPSG:4326）坐标系下，根据小区经纬度构造点几何 $P = \text{Point}(lon, lat)$。
   * 计算与网格图层多边形的拓扑相交包含：
     $$Grids_{no\_buffer} = \{ G \mid G.geometry.contains(P) \}$$
   * 记录所有命中的网格 ID（`grid_id_no_buffer`）与网格名（`grid_name_no_buffer`），多个网格以逗号分隔。
2. **缓冲500米网格匹配（投影转换 + 差集去重）**：
   * **投影变换**：将网格图层由经纬度坐标系投影转换至米制投影坐标系（如 UTM Zone 49N，EPSG:32649），避免在球面度数下做缓冲形变。
   * **缓冲区构建**：几何图形向外缓冲 500 米：$G_{buffered} = G_{projected}.buffer(500)$，再转回 EPSG:4326。
   * **差集去重**：缓冲命中网格集合中剔除已在“不缓冲网格”中命中的集合，保留纯粹因外扩 500 米而捕获的临近网格：
     $$Grids_{buffer\_500m} = Grids_{buffered} \setminus Grids_{no\_buffer}$$
3. **业务标签反查**：
   * 读取《标签匹配.xlsx》，根据命中网格 ID 检索关联其业务标签（如口碑格、高校格、交通干线格等）。

### 3.5 复合唯一键冲突判定与增量合并（Upsert）
针对单小区多天线、同站多分区分裂等复杂场景，采用复合主键作为唯一行标识：
$$\text{Unique Key} = (cgi, lon, lat, ant_dir)$$

* **全量清除模式**（勾选 `clear_existing`）：在处理文件前执行 `DELETE FROM engineering_params`，清空表后重新导入。
* **增量合并模式**（默认）：
  * 根据复合键查询数据库是否存在匹配行：
    * **存在**：更新该记录的小区名称、制式、频段、物理站信息、各角度参数、网格归属等全量属性。
    * **不存在**：实例化新对象插入数据表。

---

## 4. 汇总总工参衍生参数计算规则（Beam & Radius）

工参汇总为总工参后，系统内置了针对无线仿真和覆盖评估的特征衍生规则引擎，自动计算小区的水平半功率波瓣角（`beam`）与参考覆盖半径（`radius`）：

### 4.1 水平波束角（Beam）计算规则
* 室分站统一固定为全向覆盖：`359°`
* 5G 宏站：
  * 700M 频段：`40°`
  * 2.6G 频段：`65°`
  * 4.9G 频段：`70°`
* 4G 宏站：
  * FDD900：`30°`
  * FDD1800：`50°`
  * F频段：`45°`
  * D频段：`60°`
  * A频段：`55°`
* 其他未定义制式/频段：默认 `40°`

### 4.2 覆盖半径（Radius，单位：米）计算规则
* 室分站：统一为 `30米`
* 5G 宏站：
  * 700M 频段：`50米`
  * 2.6G 频段：`40米`
  * 4.9G 频段：`30米`
* 4G 宏站：
  * FDD900：`47米`
  * FDD1800：`43米`
  * F频段：`39米`
  * D频段：`42米`
  * A频段：`38米`
* 其他未定义制式/频段：默认 `40米`

---

## 5. 核心数据表结构定义（engineering_params）

汇总后的总工参数据统一落地到 `engineering_params` 表中：

| 字段名 | 类型 | 说明 | 约束 |
| :--- | :--- | :--- | :--- |
| `id` | INTEGER | 自增主键 | PRIMARY KEY |
| `cgi` | VARCHAR(100) | 小区全球唯一标识 (CGI) | NOT NULL |
| `celname` | VARCHAR(200) | 小区中文名 | |
| `zhishi` | VARCHAR(50) | 制式（4g / 5g） | |
| `pinduan` | VARCHAR(50) | 工作频段（如 2.6G、FDD1800 等） | |
| `phy_name` | VARCHAR(200) | 物理站/局站名称 | |
| `phy_id` | VARCHAR(100) | 物理站ID | |
| `lon` | FLOAT | 经度 (WGS84) | 联合唯一约束组成字段 |
| `lat` | FLOAT | 纬度 (WGS84) | 联合唯一约束组成字段 |
| `ant_dir` | FLOAT | 天线方位角（度） | 联合唯一约束组成字段 |
| `elect_tilt` | FLOAT | 电子下倾角（度） | |
| `mech_tilt` | FLOAT | 机械下倾角（度） | |
| `ant_height` | FLOAT | 天线挂高（米） | |
| `antenna_name`| VARCHAR(200) | 天线型号 | |
| `stauts_unit` | VARCHAR(50) | 网元状态（在网/停用等） | |
| `jifang_name` | VARCHAR(200) | 机房名称 | |
| `manufacturer`| VARCHAR(100) | 设备厂家（华为/中兴/爱立信等） | |
| `area_compy` | VARCHAR(100) | 所属区县公司 | |
| `site_type` | VARCHAR(100) | 站点类型（室外宏站/室分） | |
| `pl_item` | VARCHAR(100) | 所属规划ID/期别 | |
| `grid_id_no_buffer` | VARCHAR(100) | 不缓冲命中网格ID（逗号拼接） | |
| `grid_name_no_buffer`| VARCHAR(200) | 不缓冲命中网格名称 | |
| `grid_label_no_buffer`| VARCHAR(200) | 不缓冲网格分类标签 | |
| `grid_id_buffer_500m` | VARCHAR(100) | 缓冲500米命中网格ID（已排除不缓冲） | |
| `grid_name_buffer_500m`| VARCHAR(200) | 缓冲500米命中网格名称 | |
| `grid_label_buffer_500m`| VARCHAR(200) | 缓冲500米网格分类标签 | |
| **UNIQUE KEY** | - | `UNIQUE (cgi, lon, lat, ant_dir)` | 复合防重唯一索引 |

---

## 6. 代码落地与模块对应关系

* **前端交互与导入总控**：`app/blueprints/dashboard/routes.py`（函数：`import_engineering_data`）
* **GIS空间匹配与网格计算**：`utils/grid_matcher.py`（类：`GridMatcher`）
* **工参查询/导出/统计服务**：`app/services/gc_organizer_service.py`（类：`GCOrganizerService`）
* **Streamlit工具界面与SQL模板导出**：`tools/gc_organizer.py`（类：`GCOrganizer`）
* **ORM数据模型映射**：`app/models/engineering_params.py`（模型：`EngineeringParams`）
* **底层数据库连接管理与升级脚本**：`database.py`
