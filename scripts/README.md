# scripts/

| 目录 | 说明 |
|------|------|
| `tools/` | 一次性/辅助工具（不进入 Web 主任务） |
| `legacy/` | 已归档旧脚本，**正式流程不再引用** |

正式功能入口见项目根 `CapPhysCombine.py` 与 `app/pipelines/`。
零低流量请使用 `app/pipelines/zero_low_flow.py`（`CapPhysCombine.py zero-low-flow`）。
